spring.application.name=orcific-minutes
server.port=${SERVER_PORT:8080}

ollama.base-url=${OLLAMA_BASE_URL:http://localhost:11434}
ollama.embedding-model=${OLLAMA_EMBEDDING_MODEL:nomic-embed-text}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:sqlite:./data/meeting-notes.db}
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}

app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}

whisper.base-url=${WHISPER_BASE_URL:http://localhost:8001}
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

